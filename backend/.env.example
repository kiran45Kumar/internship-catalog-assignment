# MongoDB connection. For local dev, the docker-compose in the project root
# will start a MongoDB on this URI. If you prefer your own Mongo or Atlas,
# replace this URI.
MONGODB_URI=mongodb://localhost:27017/internship_catalog

# Port the backend listens on. The frontend expects 4000 by default.
PORT=4000
