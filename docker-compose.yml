services:
  mongodb:
    image: mongo:7
    container_name: internship_catalog_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
