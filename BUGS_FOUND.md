# Bugs Found

* [x] Internship detail page throws an error when clicking an internship (`params.id` issue in Next.js App Router).
* [x] Pagination displays incorrect number of internships (20 internships should be displayed as 9, 9, and 2 across pages).
* [ ] Docker Compose only starts MongoDB; frontend and backend are not containerized.
* [ ] Apply button still shows **Apply Now** after a successful application instead of **Applied**.
* [ ] Applications page (`/applications`) does not display the internships already applied by the current user.
